# Systemd drop-in to ensure emergency shell is accessible
# even if generators or early services fail
#
# Install to: /usr/lib/systemd/system/emergency.target.d/emergency-access.conf

[Unit]
# Reduce dependencies - emergency.target should not wait for anything
DefaultDependencies=no
