apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    # Grafana uses host networking, so localhost reaches the Prometheus server
    url: http://localhost:9090
    isDefault: true
    editable: true
