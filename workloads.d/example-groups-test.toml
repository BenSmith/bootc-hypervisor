# Test workload to verify group membership preservation
# This workload demonstrates that extra_groups are passed through to the container
#
# To test:
# 1. Enable this workload (set enabled = true)
# 2. systemctl daemon-reload
# 3. Check user groups: id _wl-groups-test-2
# 4. Check container: podman exec workload-groups-test-2 id
# 5. Should see: groups=10002(_wl-groups-test-2),44(video),987(render),996(input)

[workload]
name = "groups-test"
enabled = false  # Set to true to test group preservation

[container]
# Simple alpine container that sleeps forever (so we can exec into it)
image = "public.ecr.aws/docker/library/alpine:latest"
id = "2"  # UID 10002
command = ["sleep", "infinity"]  # Keep container running

[gpu]
type = "none"

[devices]
input = false

[security]
# Add user to system groups - these should be visible in the container
extra_groups = ["video", "render", "input"]

[network]
mode = "none"  # No network needed for this test
